[project]
name = "font-bitmap"
version = "0.1.0"
description = "Dynamic bitmap font rendering, served as a Cloudflare Python Worker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pillow",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
